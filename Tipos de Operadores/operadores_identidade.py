saldo = 1000
limite = 1000

print(saldo is limite) #Verifica se saldo ocupa o mesmo espaço de memória que limite
print(saldo is not limite) #Verifica se saldo não ocupa o mesmo espaço de memória que limite